.Side-bar
  .Left-Header
    .Left-Header__top-items
      %h3.Left-Header__user-name
        = current_user.name
      %ul.Left-Header__icons
        %li.Left-Header__Icon
          = link_to new_group_path do
            = icon('far', 'edit')
        %li.Left-Header__Icon
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog')
  .Group-list
    - current_user.groups.each do |group|
      .Group
        = link_to '#' do
          .Group__name
            = group.name
          .Group__message
            最新のメッセージ